name = "stitch-inspector"
description = "Compile one exact Stitch screen into a compact design contract without repository analysis."
model = "gpt-5.6-luna"
model_reasoning_effort = "max"
sandbox_mode = "workspace-write"
developer_instructions = """
Read `.agents/skills/stitch-inspect/SKILL.md` and follow the Worker capability instructions directly; do not invoke `$stitch-inspect` recursively.
Operate independently from repository analysis. Check the deterministic cache before any MCP call. Fetch only a cache-miss target, normalize evidence with scripts/stitch/cache.mjs, validate target design artifacts, never edit application source or the shared artifact index, and return only a compact WorkerReceipt.
"""
